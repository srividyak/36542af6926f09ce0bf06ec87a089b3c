var resultParser = function(wsResult, callback) {
  var responseData = '';

  wsResult.on('data', function(chunk) {
    responseData += chunk;
  });
  wsResult.on('end', function() {
    wsResult.content = responseData;
    callback(wsResult);
  });
}

exports.resultParser = resultParser;
